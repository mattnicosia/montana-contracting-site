{
  "name": "montana-contracting-site",
  "private": true,
  "version": "1.0.0",
  "engines": { "node": "22.x" },
  "scripts": {
    "start": "npm run build && node scripts/serve.mjs",
    "build": "node scripts/build.cjs",
    "test": "node --test tests/*.test.cjs && node scripts/verify-site.mjs"
  }
}
